# AGENTS.md

This project's full agent instructions live in **[CLAUDE.md](./CLAUDE.md)** — read that
file. It maps where the site's text and design live, how to verify a change, and how to
ship it (push to `main` → GitHub Actions builds and deploys to Neoserv; never zip/upload
by hand, never edit `dist/`, never commit `.env.local`).
